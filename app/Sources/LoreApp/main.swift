// Executable entry point. LoreRootApp lives in the LoreKit library target,
// which cannot host the app entry. Single-bootstrap invariant: see
// LoreRootApp.sharedContext.
import LoreKit

LoreRootApp.main()
